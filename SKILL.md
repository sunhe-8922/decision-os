---
name: decision-os
description: "通用决策系统——在不确定条件下做出高质量决策。适用于投资、创业、买房、跳槽、选城市、选赛道、产品战略、并购、资产配置等所有决策场景。核心方法：问题重构→目标约束→选项生成→变量情景→红队挑战→决策打分→行动验证。自动路由到领域 Plugin（ge-weidong-invest/land-valuator/sector-overview），无匹配 Plugin 时使用通用7层流程。触发词：决策、抉择、怎么选、选哪个、decision、该选A还是B。不触发：纯信息查询（"XX公司怎么样"）、闲聊、单一代码任务、数据查询、内容创作。判断标准：用户是否面临2个以上选项且需要做出取舍——是则触发，否则不触发。"
version: "1.3.0"
agent_created: true
---

# Decision OS v1.3

> 高质量决策不是寻找正确答案，而是在约束条件下，构造更好的选项，并用最小代价验证关键假设。

---

## 启动规则

当用户提出任何包含决策意图的问题时，按以下流程执行：

1. **识别决策类型** → 路由到对应 Plugin 或使用通用流程
2. **强制输出 Decision Memo** → 所有决策必须产出标准备忘录
3. **红队必选** → Type 1 决策必须完整红队，Type 2 至少假设攻击

### 不适用场景（直接拒绝路由到 Decision OS）

以下场景不触发 Decision OS，应使用其他 skill 或直接回答：
- **纯信息查询**：如"寒武纪最新财报""深圳房价多少"→ 使用 neodata-financial-search 或 WebSearch
- **内容创作**：如"帮我写个分析报告""写个公众号文章"→ 使用 khazix-writer
- **行业研究**：如"AI算力行业格局"→ 使用 sector-overview（除非用户明确在2+选项间做取舍）
- **估价任务**：如"评估这块地多少钱"→ 使用 land-valuator（除非用户在多个方案间选择）
- **闲聊/单选题**：没有实际取舍的对话

**判断标准**：用户是否面临2个以上选项且需要做出取舍——是则触发，否则不触发。

---

## Step 0：路由决策

接收问题后，先判断属于哪个领域：

**路由决策树**（按优先级依次判断）：

```
输入问题
  │
  ├─ 是否包含具体标的+投资意图？（如"寒武纪""仓位""买入"）
  │   YES → ge-weidong-invest
  │
  ├─ 是否包含土地/估价专业术语？（如"宗地""出让""抵押评估""基准地价"）
  │   YES → land-valuator
  │
  ├─ 是否请求行业全景/竞争格局？（如"行业格局""竞争态势""赛道分析"）
  │   YES → sector-overview
  │
  ├─ 是否跨域？（同时涉及2+领域，如"买房投资"）
  │   YES → 跨域协调协议（见下方）
  │
  └─ 以上均否 → 通用7层流程
```

**关键规则**：
- 如果同时匹配多个 Plugin，按"跨域"处理
- 如果用户的问题只是数据查询而非决策（如"寒武纪最新财报"），**不触发 Decision OS**，直接使用对应数据工具

**路由后不跳过 Decision OS 内核**。Plugin skill 提供领域专业知识和数据，Decision OS 提供决策框架。两者叠加，不是二选一。

### Plugin 注入协议

当路由到 Plugin skill 时，Decision OS 通过以下方式注入决策框架：

1. **调用 Plugin skill**：正常加载该 skill 的 SKILL.md，获取其领域知识、数据协议和输出模板
2. **在 Plugin 输出之上叠加 Decision OS 步骤**：
   - 用 Decision OS 的问题重构器重新定义问题（Plugin 可能看到的是"买不买"，Decision OS 看到"仓位配置是否最优"）
   - 用 Decision OS 的选项生成器扩展选项空间（Plugin 可能只给"买/不买"，Decision OS 强制生成5个选项）
   - 用 Decision OS 的红队挑战对抗 Plugin 的结论
3. **最终输出 Decision Memo**：无论 Plugin 输出什么格式，最终必须合并为标准 Decision Memo
4. **数据来源以 Plugin 为主**：Plugin 提供的领域数据（如 neodata、land-valuator 的评估参数）是主数据源，Decision OS 不重复获取

### 跨域协调协议

当问题同时涉及多个 Plugin（如"在珠海买房投资"涉及房产+投资）：

1. **主 Plugin 确定**：根据用户的核心意图确定主 Plugin（"投资"为意图 → ge-weidong-invest 为主；"自住"为意图 → land-valuator 为主）
2. **辅 Plugin 提供数据支持**：辅 Plugin 仅提供跨领域数据（如 land-valuator 提供区域地价，ge-weidong-invest 提供投资回报测算）
3. **决策框架统一**：无论几个 Plugin 参与，输出始终是一个 Decision Memo
4. **冲突处理**：如果两个 Plugin 对同一问题给出矛盾结论，在红队步骤中标注矛盾，由 CDO 综合裁决

---

## Step 1：问题重构器

多数人解决的是假问题。先重构。

### 操作

1. 提取用户原始问题
2. 判断原始问题是否是"真正问题"
3. 如果不是，重构为真正问题
4. 确定决策时限和成功标准

### 输出

```markdown
## 问题重构

- 原问题：___
- 真正问题：___
- 决策时限：___
- 成功标准：___
- 决策类型：Type 1（不可逆）/ Type 2（可逆）
```

### 判断规则

- "我要不要创业？" → 假问题。真正问题可能是"如何在未来5年获得最大财富增长？"
- "要不要买这只股票？" → 假问题。真正问题可能是"当前仓位配置是否最优？"
- "该不该跳槽？" → 假问题。真正问题可能是"什么职业路径能最大化我的长期价值？"

### 何时保留原问题 vs 重构

**保留原问题**（不做重构）：
- 原问题已经包含明确的选项取舍（如"选A还是B"）
- 原问题的约束和目标已经清晰
- 原问题是 Type 2 小决策（可逆、低风险）

**必须重构**：
- 原问题是"要不要X"的二选一框架（限制了选项空间）
- 原问题的目标模糊或互相冲突
- 原问题的约束未明确
- 原问题背后可能有更优的替代路径

### 信息不足时的边界规则

1. **追问 vs 推断**：
   - 必须追问：资金规模、决策时限、风险容忍度（这3项缺失无法推断）
   - 可推断：目标排序（根据上下文推断，标注"推断"）、约束条件（根据领域常识推断）
2. **降级策略**：如果用户未回应追问，使用"最保守假设"填充缺失项，并在 Decision Memo 中标注 `[未确认-保守假设]`
3. **最少必需信息**：资金规模 + 决策时限 + 至少1个约束 = 最小可决策信息集

---

## Step 2：目标与约束

### 目标排序

强迫用户（或从上下文推断）对目标做硬排序，并标记优先级：

```markdown
## 目标排序

1. [必须满足] ___
2. [最好满足] ___
3. [可放弃] ___
```

**关键规则**：
- 必须满足项只能有 1 个（最多 2 个）
- 如果多个目标互相冲突，必须做出取舍，不能"都要"
- 投资场景：收益目标 vs 风险容忍度 → 这是最常见的目标冲突

### 约束清单

```markdown
## 约束

| 约束维度 | 具体限制 | 是否硬约束 |
|---------|---------|-----------|
| 资金 | ___ | 是/否 |
| 时间 | ___ | 是/否 |
| 能力 | ___ | 是/否 |
| 法律 | ___ | 是/否 |
| 资源 | ___ | 是/否 |
| 家庭 | ___ | 是/否 |
| 心理承受 | ___ | 是/否 |
```

**关键规则**：
- 硬约束不可违反，直接排除不满足的选项
- 真正的瓶颈往往不是最明显的那个（例如创业的瓶颈可能是现金流而非能力）

---

## Step 3：选项生成器

**这是整个系统最值钱的一层。** 决策质量取决于选项空间。

### 操作

强制生成至少 5 个选项。禁止只给 2 个。

```markdown
## 选项空间

- Option A：___（最直觉的选择）
- Option B：___（A 的微调版）
- Option C：___（换一个思路）
- Option D：___（激进/极端选项）
- Option E：___（什么都不做 / 延迟决策）
```

### 选项生成策略

1. **A = 用户直觉**：第一反应是什么
2. **B = 优化A**：在同一方向上微调
3. **C = 换方向**：完全不同的路径
4. **D = 激进选项**：如果押注最大收益会怎样
5. **E = 不行动**：保持现状或延迟是否更优

### 示例

问题："要不要创业？"

- A：辞职创业
- B：在职兼职创业（先验证）
- C：收购成熟小业务（跳过从0到1）
- D：自由职业（无雇员模式）
- E：继续上班，用工资定投（不创业也能致富）

---

## Step 4：关键变量 + 情景推演

### 关键变量

从所有变量中提取真正影响结果的 3-5 个关键变量。

```markdown
## 关键变量

1. ___（影响权重：高/中/低）
2. ___
3. ___
4. ___
5. ___
```

**奥卡姆规则**：如果超过 5 个变量都"关键"，说明变量识别有误，需要进一步聚合。

**领域示例**：

| 决策领域 | 典型关键变量 |
|---------|-------------|
| 投资 | 行业增速、竞争格局、估值水位、政策方向、管理层质量 |
| 买房 | 人口流入、产业支撑、利率、供给量、个人收入 |
| 创业 | 市场需求强度、获客成本、现金流跑道、竞争壁垒、团队完整性 |
| 跳槽 | 行业景气度、薪资差异、成长空间、通勤成本、家庭适配 |

### 情景推演

对每个选项（至少对 Top 3 选项）构建三情景。详见 `references/scenario-analysis.md`。

```markdown
## 情景推演（Option ___）

| 情景 | 概率 | 核心假设 | 预期结果 | 触发信号 |
|------|------|---------|---------|---------|
| 乐观 | ___% | ___ | ___ | ___ |
| 中性 | ___% | ___ | ___ | ___ |
| 悲观 | ___% | ___ | ___ | ___ |

期望收益：___

二阶效应：
- 一级：___
- 二级：___
- 三级：___（Type 1 决策必填）
```

### 数据获取

根据决策领域，使用对应数据源：

| 领域 | 主数据源 | 辅助数据源 | 调用方式 |
|------|---------|-----------|---------|
| 投资 | `neodata-financial-search` + `westock-data` | WebSearch（研报/新闻） | 见下方投资数据协议 |
| 房产 | `land-valuator` + WebSearch（区域成交数据） | `neodata-financial-search`（宏观数据） | 先调用 land-valuator skill，再用 WebSearch 补充区域数据 |
| 行业 | `sector-overview` + `neodata-financial-search` | WebSearch（行业报告） | 先调用 sector-overview skill，再用 neodata 补充财务数据 |
| 职业/生活 | WebSearch（行业薪资/城市数据） | 用户自身数据 | 见下方通用搜索策略 |

### 投资数据协议

投资类决策必须使用以下数据源获取结构化数据：

```bash
# 1. 检查 NeoData token
if [ -z "$NEODATA_TOKEN" ]; then
    # 调用 connect_cloud_service 工具 → 提取 token → export NEODATA_TOKEN="<token>"
fi

# 2. 使用 neodata-financial-search skill 的查询脚本
python "<neodata-skill-dir>/scripts/query.py" --query "你的自然语言查询" --data-type api
python "<neodata-skill-dir>/scripts/query.py" --query "你的自然语言查询" --data-type doc

# 3. 使用 westock-data skill（如已安装）
# 查询个股行情、财务报表、资金流向等
```

- `<neodata-skill-dir>` = neodata-financial-search skill 的目录路径
- `--data-type api` 获取结构化数据；`--data-type doc` 获取研报/新闻
- 如果 auth 失败（HTTP 401/403），重新调用 `connect_cloud_service` 一次再重试
- **所有 NeoData 数据必须标注引用**：`NeoData Financial Search, query="<query>", <date>`

### 通用搜索策略

非投资/房产/行业类决策（职业/生活/创业），使用 WebSearch 获取数据：

```
搜索1：[领域] + [关键变量] + 最新数据/趋势
搜索2：[领域] + [地域] + 对比/排名/薪资
搜索3：[领域] + 风险/挑战/避坑
```

示例（跳槽去深圳）：
- 搜索1："深圳 互联网 薪资水平 2025"
- 搜索2："深圳 vs 北京 生活成本 对比"
- 搜索3："深圳跳槽 风险 注意事项"

---

## Step 5：红队挑战

强制反驳。这是整个系统最重要的部分。详见 `references/red-team-protocol.md`。

### 操作

1. 对 Top 3 选项执行红队攻击
2. Type 1 决策：完整三层攻击（假设/数据/风险）
3. Type 2 决策：至少 Layer 1（假设攻击）

```markdown
## 红队挑战

### 最可能失败原因（Top 3）
1. ___
2. ___
3. ___

### 最脆弱假设
___

### 缺失关键数据
1. ___
2. ___

### 被低估风险
1. ___
2. ___
```

**红队反模式警告**（避免"为红而红"）：
- ❌ "任何事情都可能出错" — 无信息量的泛泛质疑
- ❌ "黑天鹅事件" — 不可预测的事件不是红队的目标
- ✅ 针对具体假设提出具体反驳证据
- ✅ 指出数据的时间滞后或样本偏差
- ✅ 识别被系统性低估的风险（如生存者偏差）

### 并行 Agent 策略

对于重大决策（Type 1），可启动红队 Agent 并行工作：

- **数据 Agent**：使用 `neodata-financial-search` / `westock-data` / WebSearch 获取关键数据
- **红队 Agent**：独立人格，目标是否定当前最优选项
- **CDO（你自己）**：综合裁决

不需要 6 个 Agent。3 个足够，协调成本可控。

---

## Step 6：决策打分

对 Top 3 选项打分。维度压缩为 4 个（原始7维度太散）：

| 维度 | 含义 | 权重 |
|------|------|------|
| 期望收益 | 三情景加权收益 | 30% |
| 成功概率 | Base+Bull 概率之和 | 25% |
| 最大损失 | Bear 情景下的损失幅度 | 25% |
| 灵活性 | 可逆程度 + 转向成本 | 20% |

```markdown
## 决策评分

| 方案 | 期望收益(30%) | 成功概率(25%) | 最大损失(25%) | 灵活性(20%) | 加权总分 |
|------|-------------|-------------|-------------|------------|---------|
| A | /10 | /10 | /10 | /10 | /10 |
| B | /10 | /10 | /10 | /10 | /10 |
| C | /10 | /10 | /10 | /10 | /10 |
```

**注意**：最大损失是反向指标（损失越小分越高）。灵活性也是反向指标（越灵活分越高）。

### Bezos 可逆性判断

| 决策类型 | 标准 | 行动建议 |
|---------|------|---------|
| Type 1 | 不可逆或逆转代价极高 | 谨慎决策，充分红队，可延迟 |
| Type 2 | 可逆或逆转代价低 | 快速行动，边做边调 |

---

## Step 7：行动验证

决策不是终点，执行才是。

```markdown
## 行动计划

### 未来 7 天
1. ___
2. ___

### 未来 30 天
1. ___
2. ___

### 未来 90 天
1. ___
2. ___

### 验证指标
| 指标 | 当前值 | 目标值 | 检查时间 |
|------|-------|-------|---------|
| ___ | ___ | ___ | ___ |

### 止损条件
如果出现以下情况，立即重新决策：
1. ___
2. ___
```

---

## 最终输出：Decision Memo

所有决策必须输出标准 Decision Memo。这是不可省略的。

### 通用 Decision Memo 模板

```markdown
# Decision Memo

**日期**：___
**决策者**：___
**决策类型**：Type 1 / Type 2

---

## 问题
___

## 目标
1. [必须满足] ___
2. [最好满足] ___
3. [可放弃] ___

## 约束
___

## 选项
| 方案 | 一句话描述 |
|------|-----------|
| A | ___ |
| B | ___ |
| C | ___ |

## 关键变量
1. ___
2. ___
3. ___

## 情景推演（最优方案）
| 情景 | 概率 | 预期结果 |
|------|------|---------|
| 乐观 | ___% | ___ |
| 中性 | ___% | ___ |
| 悲观 | ___% | ___ |

## 红队观点
- 最可能失败原因：___
- 最脆弱假设：___

## 最终决策
___

## 置信度
___ / 10

## 执行计划
- 7天内：___
- 30天内：___

## 观察指标
1. ___
2. ___

## 止损条件
1. ___
2. ___
```

### 领域定制 Decision Memo 变体

通用模板覆盖所有场景，但以下领域有专属扩展字段，在通用模板基础上**追加**：

#### 投资类（路由到 ge-weidong-invest 时）

```markdown
## 投资专属字段

### 赛道评估
- 国家战略刚需：是/否 | 依据：___
- 行业壁垒：___年 | 类型：___
- 成长空间：___x

### 仓位建议
- 建议仓位：___% | 底仓：___% | 卫星：___%
- 入场时机：冰点信号/非冰点
- 止损线：-___% | 止盈线：+___%

### 周期定位
- 当前周期位置：冰点/左侧/拐点/右侧/过热
- 戴维斯双击概率：___%
```

#### 房产类（路由到 land-valuator 时）

```markdown
## 房产专属字段

### 用地分析
- 用途：住宅/商服/工矿仓储/其他
- 权利状态：出让/划拨/抵押/其他
- 估价方法：比较法/收益法/成本法/剩余法（需2种以上交叉验证）

### 区域数据
- 板块均价：___元/㎡ | 目标单价：___元/㎡
- 周边配套评分：___/10
- 规划利好：___

### 合规检查
- 估价规范：GB/T 18508 / GB/T 50291
- 报告类型：技术报告/客户报告
- 修正系数偏差：≤___%（超过需重算）
```

#### 职业/生活类

```markdown
## 职业/生活专属字段

### 个人资产盘点
- 当前年收入：___ | 储蓄：___ | 负债：___
- 核心技能变现力：___/10
- 人脉杠杆：___/10

### 城市对比（如涉及城市选择）
| 维度 | 城市A | 城市B |
|------|-------|-------|
| 行业机会 | ___ | ___ |
| 生活成本 | ___ | ___ |
| 教育资源 | ___ | ___ |
| 通勤/宜居 | ___ | ___ |

### 隐性成本
- 社交重建成本：___
- 家庭适应难度：___/10
- 3年回退成本：___
```

---

## 复盘系统（跨决策元系统）

复盘不属于单次决策流程，而是跨决策的元系统。每次重大决策结果出来后，使用以下模板复盘：

```markdown
# Decision Review

**原决策日期**：___
**复盘日期**：___

| 维度 | 内容 |
|------|------|
| 原假设 | ___ |
| 实际结果 | ___ |
| 假设是否成立 | ✅ / ❌ |
| 错误来源 | 认知错误 / 信息错误 / 执行错误 / 运气因素 |
| 关键教训 | ___ |
| 下次改进 | ___ |
```

### 复盘触发规则

复盘不应靠记忆，应有明确触发机制：

1. **定时复盘**：在 Decision Memo 的"观察指标"中设定检查时间点，到达时自动触发复盘
2. **止损触发**：当止损条件被触发时，立即复盘原决策
3. **重大事件**：当关键变量发生超出情景推演范围的变化时，复盘原决策假设
4. **最低复盘频率**：
   - Type 1 决策：每30天至少复盘1次，直到结果确定
   - Type 2 决策：每90天复盘1次，或当结果明显偏离预期时

### 复盘结果归档

复盘结果必须更新到工作区 memory 中（`MEMORY.md` 或当日日志），格式：

```
## Decision Review: [决策标题]
- 日期：___
- 原假设→实际：___
- 教训：___
- 改进：___
```

形成决策知识库，避免同类错误重复。

### 复盘→Skill 更新闭环

复盘不仅要记录，还要**驱动 Decision OS 自身进化**：

1. **模式识别**：如果同类决策连续3次出现相同类型的错误（如投资决策总是低估政策风险），说明 Decision OS 的红队协议有盲区
2. **触发 Skill 更新**：当复盘发现以下情况时，必须更新 Decision OS SKILL.md：
   - 新的领域需增加专属模板变体（如连续3次创业决策后，提炼创业专属字段）
   - 路由规则有误（如某类问题反复被错误路由）
   - 红队盲区（某个风险类别在3+次决策中被遗漏）
   - 选项生成器不够（某领域总是只能想到3个选项而非5个）
3. **更新格式**：在 SKILL.md 末尾的"设计哲学"之前，追加 `## 变更日志`：

```markdown
## 变更日志

| 版本 | 日期 | 变更 | 触发原因 |
|------|------|------|---------|
| v1.3 | 2026-06-04 | 新增创业专属模板 | 3次创业决策复盘发现通用模板遗漏团队维度 |
```

4. **版本递增规则**：每次 Skill 更新后 version 递增 0.1（如 v1.2→v1.3）

---

## 插件架构

```
Decision OS（统一决策内核）
├── Investment Plugin → ge-weidong-invest
├── Real Estate Plugin → land-valuator
├── Sector Research Plugin → sector-overview
├── Consulting Plugin → 待建
├── Business Plugin → 待建
├── Career Plugin → 待建
└── Life Plugin → 待建
```

详见 `references/plugins.md`

---

## 信源优先级

执行数据获取时，严格遵守用户定义的信源优先级：

| 信息类型 | 一手来源（优先） |
|---------|----------------|
| 产品更新/技术决策 | 官方博客、GitHub Release Notes、创始人推文 |
| 融资/商业数据 | 公司官方公告、SEC/工商文件 |
| 用户口碑 | GitHub Issues、Reddit讨论、Twitter/X、知乎帖子 |
| 行业分析 | 权威媒体原创报道（非转载） |
| 学术/技术原理 | arXiv论文、Google Scholar、学术会议论文集 |

---

## 设计哲学

1. **选项空间决定决策质量** — 不是分析决定质量，是你看到了多少选项
2. **红队是安全阀** — 不经红队的决策都是自嗨
3. **约束是真正的边界** — 突破约束的方案不是好方案
4. **最小代价验证关键假设** — 不确定就先验证，别All-in
5. **7层而非12层** — 层太多=没有层，每层必须可执行

---

## 变更日志

| 版本 | 日期 | 变更 | 触发原因 |
|------|------|------|---------|
| v1.0 | 2026-06-04 | 初始版本，7层决策框架 | 用户设计文档 |
| v1.1 | 2026-06-04 | 触发词收窄+不适用场景+Plugin注入协议+数据获取命令+追问边界 | 自动优化循环：8维评估63.75%→80% |
| v1.2 | 2026-06-04 | 复盘触发规则+归档机制 | 自动优化循环：迭代复盘维度提升 |
| v1.3 | 2026-06-04 | 路由决策树+领域定制模板+关键变量领域示例+红队反模式+复盘→Skill闭环 | 自动优化循环：83.75%→90%+ |
